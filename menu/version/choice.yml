#!/bin/bash
#
# Title:      PTS Updater
# Author(s):  Admin9705 - Deiteq
# URL:        https://pgblitz.com - http://github.pgblitz.com
# GNU:        General Public License v3.0
################################################################################
---
- hosts: localhost
  gather_facts: false
  tasks:
    - name: Register Project
      shell: 'cat /var/plexguide/pg.number'
      register: serverid

    - name: 'Installing Version {{serverid.stdout}}'
      git:
        repo: 'https://github.com/PTS-Team/PTS-Team'
        dest: '/opt/plexguide'
        version: '{{serverid.stdout}}'
        force: yes
      when: not serverid.stdout == "edge"
      ignore_errors: True

    - stat:
        path: /opt/plexguide
      register: p
    - debug:
        msg: "Path exists and PTS folder exists"
      when: p.stat.isdir is defined and p.stat.isdir

    - name: 'Setting ownership on PTS Folder'
      shell: 'chown -R 1000:1000 /opt/plexguide'

    - name: 'Setting permissions on PTS Folder'
      shell: 'chmod -R 775 /opt/plexguide'

    - name: 'Stops First Time Run'
      shell: 'touch /var/plexguide/ask.yes'
      register: program